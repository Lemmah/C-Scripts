#include <cs50.h>
#include <string.h>
#include <stdio.h>

int main(int argc, string argv[])
{
    // looping through the argument strings and printing each of them
    for (int i = 0, j = argc; i < j; i++)
    {
        printf("%s\n", argv[i]);
    }
}
